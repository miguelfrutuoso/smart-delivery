import { DateTranformPipe } from './date-tranform.pipe';

describe('DateTranformPipe', () => {
  it('create an instance', () => {
    const pipe = new DateTranformPipe();
    expect(pipe).toBeTruthy();
  });
});
